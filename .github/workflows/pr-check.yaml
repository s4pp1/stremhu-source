name: PR Check

on:
  pull_request:
    branches: [ main ]
    paths:
      - "client/**"
      - "server/**"
      - "relay/**"
      - ".github/workflows/**"

jobs:
  server:
    name: Server
    uses: ./.github/workflows/reusable-test-and-build.yaml
    with:
      working-directory: ./server
    secrets: inherit

  client:
    name: Client
    uses: ./.github/workflows/reusable-test-and-build.yaml
    with:
      working-directory: ./client
    secrets: inherit
